version: '3.8'
services:
  nextjs-app:
    build:
      context:
      dockerfile: Dockerfile
    container_name: nextjs_app
    ports:
      - "4000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - .:/app
      - /app/node_modules
